import CategoryLayout from './CategoryLayout'

export const generated = (args) => {
  return <CategoryLayout {...args} />
}

export default {
  title: 'Layouts/CategoryLayout',
  component: CategoryLayout,
}
